{
  "name": "face-count-eval",
  "private": true,
  "type": "module",
  "description": "Offline face-count detection accuracy evaluation (#1222) - runs the actual production model in a real headless browser.",
  "scripts": {
    "postinstall": "node setup-mediapipe-assets.mjs",
    "eval": "node eval.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.22-rc.20250304"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
